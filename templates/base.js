// JavaScript to toggle the sidebar when clicking the hamburger icon
document.querySelector(".hamburger").addEventListener("click", function () {
    document.querySelector(".sidebar").classList.toggle("open");
});
